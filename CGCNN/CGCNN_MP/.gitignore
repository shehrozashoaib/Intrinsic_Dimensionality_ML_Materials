__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Conda env (shipped to nodes, not tracked)
envs/

# Cached MP tensors / split tensors (large, regenerable)
cached_mp/
cached_tensors*/
root_dir/

# Raw per-run sweep outputs (curated copies live under results/)
results_mp_eform_fastfood/
results_mp_bandgap_fastfood/
results/*/run_*/

# Slurm / driver logs (root only; keep curated results/<exp>/logs/)
/logs/

# Large datasets
*.jsonl
id_prop.json
*.npz
*.pt
*.pth
*.pth.tar

# Local environment/editor files
.env
.venv/
venv/
.vscode/
.idea/

# DCGM GPU-monitoring stats
dcgm/
